#include<iostream>
using namespace std;
int main()
{
    int arr[10],i,sum=0;
    cout<<"Enter the 10 numbers :"<<endl;
    for(i=0;i<10;i++)
    cin>>arr[i];
    for(i=0;i<10;i++)
    {
        sum=sum+arr[i];
    }
    cout<<"sum of given numbers = "<<sum;
}
